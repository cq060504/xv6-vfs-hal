# xv6 双架构启动、串口、跳板与中断说明

本文面向刚开始阅读体系结构代码的读者，结合当前工程的 RISC-V 和
LoongArch 实现，解释以下四条主线：

1. CPU 从 QEMU 启动到进入 `scheduler()`。
2. 一个字符如何通过 UART 在终端和 xv6 之间传递。
3. 用户程序如何进入内核，又如何通过跳板返回用户态。
4. PLIC/EIOINTC 如何把外部中断交给通用内核。

阅读时先记住一个总原则：

> 通用内核决定“发生中断后做什么”，HAL 决定“该架构如何进入中断、
> 读取硬件状态以及返回”。

---

## 1. 先建立一套共同概念

### 1.1 CPU、hart 与特权级

RISC-V 把一个可独立执行指令流的硬件线程称为 **hart**。在本项目中，
可以把 hart 和 LoongArch 的 CPU core 暂时理解为同一类对象。

两种架构使用不同的特权级名称：

| 含义 | RISC-V | LoongArch |
| --- | --- | --- |
| 最高特权 | M-mode | PLV0 |
| 内核运行级别 | S-mode | PLV0 |
| 用户运行级别 | U-mode | PLV3 |

本项目中的明显区别是：

- RISC-V 先在 M-mode 启动，再由 `mret` 降到 S-mode 执行内核。
- LoongArch 由 QEMU 直接在 PLV0 启动，内核一直使用 PLV0。

### 1.2 CSR、UART、MMIO

**CSR** 是 CPU 内部的控制/状态寄存器。

- RISC-V 
  - `sstatus`（记录并控制处理器的当前状态）      
  - `stvec`（存储内核陷阱处理程序的基地址，常指向 trampoline 页中的 uservec 代码）、
  - `sepc`（保存触发陷阱时的程序计数器）、
  - `scause`（记录触发陷阱的具体原因）、
  - `satp`（通过 satp 切换内核与用户页表，只有设置了 satp，CPU 才会开启虚拟内存转换机制）。

- LoongArch 
  - `CRMD`（当前模式信息，PLV,DA与PG，全局中断使能IE）:
     - IE处理器异常的开关，只有当 IE=1 且存在待处理中断（由 ESTAT 和 ECFG 共同决定）时，CPU 才会暂停当前指令流并跳转到异常入口
  - `EENTRY`（异常入口基址，TLB Refill 异常有专门的入口寄存器 TLBRENTRY，不受此寄存器配置影响）
  - `ERA`（将当前触发异常的指令 PC 保存，如果是 TLB Refill 异常，地址会保存在 TLBRERA 而非此寄存）
  - `ESTAT`（异常状态）
  - `PGDL`/`PGDH`（页表根寄存器，分别管理低地址和高地址的页表）
  - `CSR.PWCL` /`CSR.PWCH`(页表遍历参数寄存器，分别管理低地址和高地址的页表的层级和宽度)、
  - `PRMD`(保存异常前的 PLV 与 IE 状态)
  - `CSR.DMW0 - DMW3`(直接映射窗口寄存器，允许 PLV0 直接访问低端物理内存)
  -  `BADV`（保存异常相关的地址）
     - 如果是 TLB Refill 异常，地址会保存在 TLBRBADV 而非此寄存器;
     - 不同于ERA，BADV仅限于地址错误（ADE）、缺页（PI）、权限非法（PPI）等访存相关异常，ERA保存的是触发异常的指令地址，
     -  假设地址 0x4000 执行指令 LD.W $r1, $r2, 0，而 $r2 此时指向一个未分配的地址 0x8000。ERA 会记录 0x4000（指令地址）。BADV 会记录 0x8000（报错的访存地址）。
  - `ECFG`（中断管理的“分流器”,异常配置寄存器，决定哪些异常类型会触发中断）
     - LIE [12:0]：局部中断使能。对应 ESTAT 中的 13 个中断源，只有这里置 1，对应的硬件中断或定时器中断才会被响应。
     - VS [18:16]：配置异常入口的间距。如果设为 0，所有异常共用一个入口；如果不为 0，不同异常会按间距排列在 EENTRY 之后
   
**UART** 是一个常见的串口外设，负责在 CPU 和终端之间传输字符。
 - CSR ：是 CPU 核心内部的寄存器
 - UART 寄存器：是 CPU 外部硬件设备的寄存器。它们通过总线连接，并通过 MMIO 映射到内存地址空间中。

**MMIO** 将硬件外设（如 UART、中断控制器）的寄存器映射到 CPU 的物理地址空间中，而虚拟地址空间通过 SUC 访存属性 确保这种读写是安全且符合时序的
。硬件控制器把自己伪装成了一块内存。 操作这块“伪装内存”，实际上是在拨动硬件的开关

- RISC-V UART 基址：`0x10000000`。
- LoongArch UART 基址：`0x1FE001E0`。

代码中的 `volatile` 很重要，它要求编译器真的执行每一次设备读写，
不能把这些访问缓存或优化掉。

**PLIC(riscv) PCH-PIC(LA) 与 UART 的联系**
 - 触发：当用户在键盘上敲击一个字符时，UART 硬件产生一个中断信号。
 - 路由：这个信号首先发送到 PLIC (平台级中断控制器)。
 - 分发：PLIC 作为一个调度中心，根据优先级和屏蔽设置，决定将这个中断交给哪个 CPU 核心处理。
 - 处理：CPU 收到 PLIC 的通知后，跳转到陷阱处理程序，通过读写 UART 的 MMIO 寄存器来取回字符。

无论是 PLIC 还是 LA 的 EIOINTC ，它们在系统中的生态位是一样的——都是负责把像 UART 这样众多的外设中断请求，有序地路由给 CPU。

### 1.3 trap、exception 与 interrupt

`trap` 是“控制流从当前程序转入内核处理入口”的总称：

- **exception**：同步异常，由当前指令导致，例如系统调用、缺页。
- **interrupt**：异步中断，由定时器或外部设备导致。

硬件发生 trap 时只保存少量控制状态，例如异常 PC 和原因。硬件不会
自动替内核保存全部通用寄存器，因此必须有一段汇编入口尽快保存现场。

### 1.4 trapframe 与 context 不相同

`trapframe` 保存用户程序发生 trap 时的完整用户寄存器现场，用于将来
恢复用户程序。

`context` 用于内核线程之间执行 `hal_switch()`，只保存：

- 返回地址；
- 栈指针；
- ABI 规定的 callee-saved 寄存器。

因此 `trapframe` 比 `context` 大得多。本文讨论用户/内核跳板时，重点是
`trapframe`，不是调度器的 `context`。

### 1.5 中断需要通过多道“开关”

一个 UART 中断能到达内核，通常需要同时满足：

1. UART 自身允许产生接收中断。
2. 外部中断控制器允许并路由该 IRQ。
3. CPU 允许接收这一类中断。
4. CPU 的全局中断位打开。
5. 中断入口地址已经正确设置。

只打开其中一个开关并不够。排查“设备有数据但内核没收到中断”时，
应沿着这五层逐级检查。

---

## 2. 工程中的 HAL 边界

与本文最相关的 HAL 接口是：

- [`hal/hal_console.h`](hal/hal_console.h)：控制台和 UART。
- [`hal/hal_intr.h`](hal/hal_intr.h)：外部中断控制器。
- [`hal/hal_arch.h`](hal/hal_arch.h)：trap 状态、页表根、架构寄存器与
  基础指令。
- [`hal/hal_vm.h`](hal/hal_vm.h)：TLB 与虚拟内存抽象。

通用层主要位于：

- [`kernel/main.c`](kernel/main.c)：各子系统的启动顺序。
- [`kernel/console.c`](kernel/console.c)：控制台字符队列和行编辑。
- [`kernel/trap.c`](kernel/trap.c)：系统调用、中断和异常的通用分发。
- [`kernel/proc.c`](kernel/proc.c)：进程、用户页表和 trapframe。

同一个通用调用在两个架构下链接到不同实现。例如
`hal_irq_claim()`：

- RISC-V 链接到 PLIC 的 claim 寄存器操作。
- LoongArch 链接到 EIOINTC 的 ISR 位图读取。

这正是 HAL 的价值：`kernel/trap.c` 不需要理解两个控制器的寄存器布局。

---

## 3. CPU boot：从 QEMU 到 scheduler

### 3.1 两个架构共享的后半段

无论前半段如何启动，最后都会进入 [`kernel/main.c`](kernel/main.c)。
CPU 0 依次初始化：

```text
consoleinit()
  -> printfinit()
  -> kinit()
  -> kvminit()
  -> kvminithart()
  -> procinit()
  -> trapinit()
  -> trapinithart()
  -> hal_irq_init()
  -> hal_irq_hart_init()
  -> 文件、VFS 和块设备初始化
  -> hal_timer_init()
  -> userinit()
  -> scheduler()
```

其他 CPU 等待 CPU 0 完成全局初始化，然后只执行本 CPU 所需的：

```text
kvminithart()
  -> trapinithart()
  -> hal_irq_hart_init()
  -> scheduler()
```

这个顺序有明确的稳定性要求：

- 必须先准备页表，再依赖虚拟地址运行。
- 必须先设置 trap 入口和中断控制器，再开放正常的中断处理。
- 全局数据结构只由 CPU 0 初始化一次。
- 每 CPU 的页表寄存器、trap 入口和中断上下文需要每个 CPU 各自设置。

### 3.2 RISC-V 启动过程

相关文件：

- [`hal/riscv/kernel.ld`](hal/riscv/kernel.ld)
- [`hal/riscv/hal_entry.S`](hal/riscv/hal_entry.S)
- [`hal/riscv/hal_start.c`](hal/riscv/hal_start.c)
- [`kernel/main.c`](kernel/main.c)

#### 第一步：QEMU 装载内核

QEMU `virt` 机器把 `-kernel` 指定的内核放到物理地址
`0x80000000`。链接脚本也让 `_entry` 位于该地址，因此 CPU 的第一条
指令能够落在 [`hal/riscv/hal_entry.S`](hal/riscv/hal_entry.S)。

此时：

- CPU 位于 M-mode。
- 分页尚未开启，地址可按物理地址理解。
- C 语言运行环境还没有完全建立，首先要准备栈。

#### 第二步：为每个 hart 分配启动栈

`_entry` 读取 `mhartid`，根据 hart 编号在 `stack0` 数组中选取自己的
4 KiB 启动栈：

```text
sp = stack0 + (mhartid + 1) * 4096
```

随后调用 C 函数 `start()`。不同 hart 使用不同栈，否则多个 CPU 的
函数调用会互相覆盖返回地址和局部变量。

#### 第三步：从 M-mode 准备进入 S-mode

[`hal/riscv/hal_start.c`](hal/riscv/hal_start.c) 中的 `start()` 完成：

1. 把 `mstatus.MPP` 设置为 S-mode。
2. 把 `mepc` 设置为 `main`。
3. 令 `satp=0`，继续暂时关闭分页。
4. 把异常和中断委托给 S-mode。
5. 配置 PMP，使 S-mode 可以访问所需物理内存。
6. 准备 S-mode 定时器能力。
7. 把 hart id 写入 `tp`。
8. 执行 `mret`。

`mret` 不只是“函数返回”。它根据 `mstatus.MPP` 切换特权级，并跳转到
`mepc`，所以执行结果是：

```text
M-mode start() --mret--> S-mode main()
```

#### 第四步：建立内核页表

`main()` 中的 `kvminit()` 在内存中建立内核页表，
`kvminithart()` 再把根页表写入 `satp` 并刷新 TLB。从这里开始，内核
主要按虚拟地址运行。

RISC-V 内核页表映射内核代码、RAM 和设备 MMIO。每个进程有独立用户
页表，但内核态运行时切换到共享内核页表。

RISC-V 的 `start()` 已经为 `stimecmp` 写入第一次比较值，并打开
S-mode 的定时器类别位；但此时 CPU 的全局 S-mode 中断仍关闭，因此
不会在 `stvec` 和通用内核尚未准备好时闯入 trap。`main()` 完成 trap
初始化后还会调用 `hal_timer_init()` 重新安排比较值。

### 3.3 LoongArch 启动过程

相关文件：

- [`hal/loongarch/kernel.ld`](hal/loongarch/kernel.ld)
- [`hal/loongarch/hal_entry.S`](hal/loongarch/hal_entry.S)
- [`hal/loongarch/hal_start.c`](hal/loongarch/hal_start.c)
- [`kernel/main.c`](kernel/main.c)

#### 第一步：QEMU 从 BIOS 地址执行

LoongArch 版本通过 `-bios` 装载，入口位于物理地址
`0x1c000000`。链接脚本将启动代码、TLB refill 入口和 trampoline
安排在 BIOS ROM 可执行区域中。

与 RISC-V 不同，QEMU 已让 CPU 处于 PLV0 和直接地址模式，代码不需要
先经历 M-mode 到 S-mode 的降级。

#### 第二步：建立每核启动栈

[`hal/loongarch/hal_entry.S`](hal/loongarch/hal_entry.S) 读取 CPU id，
根据编号选择 `stack0` 中的启动栈，然后调用 `start()`。目的与 RISC-V
完全相同：进入 C 函数前，每个 CPU 必须拥有独立且有效的 `sp`。

#### 第三步：把可写数据搬到 RAM

BIOS ROM 可执行但不适合作为可写数据区。因此 LoongArch 链接脚本把
`.data` 的装载地址和运行地址分开：

```text
装载地址 LMA：位于 BIOS 镜像(ROM 硬件本质上是“只读”的)
运行地址 VMA：0x00400000 起的 RAM
```

CPU 0 在 `start()` 中：

1. 使用早期 UART 输出启动信息。
2. 把 `.data` 从 BIOS 区复制到 RAM。
3. 清零 `.bss`。
4. 发布 `boot_done`，允许其他 CPU 继续。

这里只能由 CPU 0 搬运一次。其他 CPU 必须等待，否则可能在 `.data`
尚未复制完成时读取全局变量。

#### 第四步：直接进入 PLV0 main

LoongArch 在 `start()` 中提前设置 `EENTRY=kernelvec`，随后直接调用
`main()`。它没有与 RISC-V `mret` 对应的降级步骤：

```text
PLV0 start() --> PLV0 main()
```

`kvminithart()` 随后配置：

- `PGDL`：低地址页表根；
- `PGDH`：高地址内核页表根；
- DMW0：PLV0 对低端物理内存的直接映射窗口；
- 四级页表遍历参数；
- TLB refill 入口。

LoongArch 的用户地址空间主要由 `PGDL` 管理，高地址内核栈等映射由
`PGDH` 管理；PLV0 还可通过 DMW0 访问低端物理地址。

### 3.4 启动差异总结

| 项目 | RISC-V | LoongArch |
| --- | --- | --- |
| QEMU 装载方式 | `-kernel` | `-bios` |
| 内核入口 | `0x80000000` | `0x1c000000` |
| 初始特权级 | M-mode | PLV0 |
| 内核运行特权级 | S-mode | PLV0 |
| 进入 `main` | `mret` 降级 | 直接调用 |
| `.data` | 与内核一起位于 RAM | 从 BIOS 复制到 RAM |
| 初始分页 | 关闭 | 直接地址模式 |
| 页表根 | `satp` | `PGDL`/`PGDH` |

---
**1. UART 到底是什么**

UART 可以理解为一个“字节收发设备”。

真实硬件中，它把 CPU 写入的一个字节转换成串行比特发送出去，也把串行输入重新组合成字节。当前项目运行在 QEMU 中，没有真实串口线，因此 QEMU 模拟 UART：

```text
xv6 向 UART 写字符 -> QEMU UART -> 宿主机终端显示
宿主机键盘输入     -> QEMU UART -> xv6 从 UART 读取
```

CPU 不直接操作终端窗口，只能读写 UART 的设备寄存器。

**2. CPU 如何访问 UART**

UART 使用 MMIO，即“设备寄存器占用一段物理地址”。

| 架构 | UART 基地址 | UART 中断号 |
|---|---:|---:|
| RISC-V | `0x10000000` | IRQ 10 |
| LoongArch | `0x1FE001E0` | IRQ 31 |

这些定义分别位于 [RISC-V memlayout.h](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/riscv/memlayout.h:18) 和 [LoongArch memlayout.h](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/loongarch/memlayout.h:23)。

驱动中的：

```c
#define Reg(reg) ((volatile unsigned char *)(UART0 + (reg)))
#define ReadReg(reg) (*(Reg(reg)))
#define WriteReg(reg, v) (*(Reg(reg)) = (v))
```

意思是：

```text
ReadReg(5)      -> CPU 读取 UART0+5 的设备寄存器
WriteReg(0, c)  -> CPU 把字符 c 写入 UART0+0
```

`volatile` 告诉编译器：这是设备访问，每次读写都必须真正执行。

**3. UART 寄存器是什么意思**

| 寄存器 | 含义 |
|---|---|
| `RHR` | Receive Holding Register，CPU 从这里读取收到的字符 |
| `THR` | Transmit Holding Register，CPU向这里写入待发送字符 |
| `IER` | Interrupt Enable Register，决定 UART 可以产生哪些中断 |
| `ISR` | Interrupt Status Register，说明 UART 为什么产生中断 |
| `FCR` | FIFO Control Register，启用或清空收发队列 |
| `LCR` | Line Control Register，设置字符长度、波特率配置模式 |
| `LSR` | Line Status Register，告诉 CPU 当前能不能收发 |

最重要的 LSR 状态位：

```text
LSR bit 0 = 1：UART 已收到字符，可以读取 RHR
LSR bit 5 = 1：THR 已空，可以写入下一个字符
```
硬件内部其实有 DLL、DLM、IER、RHR 等多个独立寄存器，但在地址解码电路（Address Decoder）中，它们被接在了同一个地址译码线上
。只要 DLAB 位一变，译码电路就会像火车的道岔一样，把 CPU 的访问请求导向不同的物理寄存器。
RHR 和 THR 都位于偏移 0，但一个用于读、一个用于写，所以不冲突。

FIFO 是 UART 内部的小队列。CPU 一时没有处理字符时，FIFO 可以暂存几个字节，避免字符立刻丢失。

**4. console 和 UART 不是同一层**

UART 只认识字节，不理解“命令行”。

你可以把这个过程想象成一个**“指挥部（CPU）”**、一个**“发报机（UART）”**以及一套**“报警系统（中断）”**之间的协作。为了理解这三者如何“各司其职”，我们可以从三个阶段来拆解：

### 补充 1. 软件（Console）如何控制硬件（UART）
软件并不认识什么“发报机”，它只认识**地址**。
*   **MMIO 与门牌号**：在 LoongArch 中，UART 寄存器被映射到物理地址（例如 `0x1fe001e0`）。内核中的 Console 驱动通过普通的载入/存储指令（如 `ST.B`）往这个地址写数据，就是在给 UART 下令。
*   **SUC 的交通规则**：关键点在于，Console 访问这些地址时必须使用 **SUC（强序非缓存）** 模式。这确保了 CPU 不会因为“自作聪明”地进行预读（投机执行）而导致数据被意外弹出 FIFO，也保证了写入的顺序严格符合逻辑。
*   **具体指令**：软件通过设置 **LCR[DLAB]** 位来切换“行政办公室”，配置波特率后再切回数据模式发送字符。

### 2. 硬件（UART）如何提醒 CPU（中断）
UART 就像是一个守卫，当它发现“有客到”（接收到字符）或“粮草尽”（发送缓冲区空）时，它不会坐等 CPU 来看，而是直接拉响警报。
*   **信号源头**：UART 产生一个 **IRQ（中断请求）** 信号。
*   **报警链路**：这个信号不是直连 CPU 的，而是要经过层层上报：
    1.  信号首先进入 **PCH-PIC**（桥片中断控制器）。
    2.  根据路由配置，转发给 **EXTIOI**（扩展 I/O 中断控制器）。
    3.  EXTIOI 将信号最终映射到 CPU 核心的 **HWI（硬件中断引脚）** 之一。
*   **账本记录**：CPU 核心感知到引脚电平变化，会在 **`CSR.ESTAT.IS`** 寄存器的对应位打上勾，告诉 CPU：外外挂设备有急事。

### 3. CPU 的“自动反应”与“软件处理”
这是最精妙的地方：CPU 硬件在检测到中断时，会**强制执行**一套预设的动作。
*   **硬件快照（瞬间动作）**：
    1.  **保存现场**：CPU 自动把当前的运行状态从 **`CSR.CRMD`** 复制到 **`CSR.PRMD`**。
    2.  **断点存证**：把当前正在执行的 PC 指针存入 **`CSR.ERA`**（以便回头能找回来）。
    3.  **屏蔽干扰**：自动将全局中断使能 **`IE`** 置为 0，防止处理一半时被另一个中断插队。
    4.  **强制跳转**：硬件直接将 PC 指针指向 **`CSR.EENTRY`** 里填写的内核入口地址。
*   **软件派发（内核接手）**：
    此时，控制权回到了内核手中。内核代码读取 **`CSR.ESTAT`** 确认是 HWI 中断，并调用 `hal_irq_claim()` 最终定位到是 UART 触发的警报。Console 驱动随后从 UART 的 **RHR** 寄存器中取走字符，并放入 `cons.buf` 缓冲区供上层使用。

**总结一下：**
1.  **软件控制硬件**：靠的是 **SUC 模式下的地址读写**。
2.  **硬件提醒 CPU**：靠的是 **中断控制器链路将信号层层上报**。
3.  **CPU 响应提醒**：靠的是 **硬件状态自动平移（CRMD->PRMD）和 PC 跳转**。

### 相关函数
- riscv
  - hal_console_init()：初始化 UART，设置波特率和中断使能。通过 MMIO 写入 LCR、IER、FCR 等寄存器。通过 hal_irq_init() 注册 UART 中断号到 PLIC。 通过 LCR [DLAB] 从设置波特率模式到数据模式。
  - hal_console_write()系统调用 write 路径：write(1, buf, 100) → consolewrite() → hal_console_write() → 逐字节发送。每发送一个字节，标记 tx_busy=1，然后 sleep 等 UART TX 完成中断。中断来后 tx_busy=0 → 进程被唤醒 → 发下一个字节。
  - hal_putchar()：printf路径
    - 和 hal_console_write 的根本区别： printf 不能 sleep——它可能在中断上下文中被调用（如 panic），也可能在持锁时被调用。所以 printf 走 hal_putchar：直接忙等 
  LSR TX_IDLE，不通过中断。 不走 tx_lock，不走 sleep。
  - hal_console_intr()：中断有两种触发原因，同时处理：
    - TX 中断： UART 发完了一个字节 → LSR_TX_IDLE=1 → 清 tx_busy → 唤醒发送线程
    - RX 中断： 用户敲了键盘 → LSR_RX_READY=1 → 读 RHR → 调用 handler(c) → 存入 console 输入缓冲

- loongarch
  -  hal_console_init() 寄存器宏定义和 RISC-V 完全相同，不再重复。关键差异在于没有 tx_lock 和 tx_busy。
  -  hal_console_write() 没有锁，没有 sleep。write() 系统调用会在这里忙等，直到所有字节发完。RISC-V 版本在发送期间会 sleep 让出 CPU——LoongArch 版本一直占着 CPU 等LSR
  - 和 RISC-V 完全一致——读 ISR，循环读 RHR，调用 handler。区别是没有 TX 完成的处理（因为没开 TX 中断）。
  -  hal_console_poll()（）核主动调用 hal_console_poll(consoleintr)，检查 UART 是否有待读字节。这是 RX 中断不可靠时的兜底方案。

```txt
console 函数和 HAL UART 函数的层次关系

  ┌──────────────────────────────────────────────────────────────┐
  │  console.c — 行编辑层（与硬件无关，纯逻辑）                     │
  │                                                              │
  │  consoleinit()    → 注册 console 为设备，初始化锁和 UART       │
  │  consolewrite()   → write() 系统调用，批量输出                 │
  │  consoleread()    → read() 系统调用，从缓冲取一行               │
  │  consoleintr()    → 每个输入字符的行编辑（回显/退格/杀行）       │
  │  consputc()       → 输出一个字符（printf 和 consoleintr 共用）  │
  └──────────┬───────────────────────────────────┬───────────────┘
             │ 输出                                │ 输入
             ▼                                     ▼
  ┌──────────────────────────────────────────────────────────────┐
  │  hal_uart.c — 硬件驱动层（操作 16550a 寄存器）                 │
  │                                                              │
  │  hal_console_init()    → 波特率/FIFO/中断 初始化               │
  │  hal_console_write()   → 逐字节发送（持锁 + sleep / 忙等）     │
  │  hal_putchar()         → 单字节轮询发送（printf 用）           │
  │  hal_console_intr()    → 中断/轮询 到达时读 UART 输入字节       │
  └──────────┬───────────────────────────────────┬───────────────┘
             │ MMIO                                │
             ▼                                     ▼
  ┌──────────────────────────────────────────────────────────────┐
  │  16550a UART 硬件 (THR/RHR/IER/FCR/LCR/LSR)                  │
  └──────────────────────────────────────────────────────────────┘

  输出路径的映射

  printf("hello\n")
    → consputc('h')  consputc('e')  ...  consputc('\n')
         → hal_putchar('h')                                  // 单字节，LSR 轮询，不 sleep
              → while(LSR & TX_IDLE == 0) ;  WriteReg(THR, 'h')

  write(1, buf, 1000) 系统调用
    → consolewrite(user_src, src, 1000)
         → hal_console_write(buf, 32)                        // 批量，最多 32 字节一块
              → acquire(&tx_lock)                            // [仅 RISC-V]
              → 逐字节: 等 tx_busy==0 → WriteReg(THR) → tx_busy=1 → sleep
              → release(&tx_lock)

  输入路径的映射

  键盘敲 'a'
    → UART 硬件收到 → RHR 寄存器有数据

    【RISC-V】
    → LSR_RX_READY=1 → UART IRQ → PLIC → devintr
    → hal_console_intr(consoleintr)   ← consoleintr 是回调函数指针
         → uart_getc() → ReadReg(RHR) → 得到 'a'
         → consoleintr('a')            ← 调用回调
              → cons.buf[] ← 'a'
              → consputc('a') → hal_putchar('a') → 回显
              → wakeup(&cons.r)                           // 唤醒 consoleread

    【LoongArch】— 双路径
    路径 A（中断，如果可靠）：
    → 同 RISC-V 中断链

    路径 B（轮询兜底）：
    → clockintr() 每 10ms：
         → hal_console_poll(consoleintr)
              → hal_console_intr(consoleintr)  ← 主动调用！不是响应中断！
                   → uart_getc() → 如果有数据 → consoleintr(c)

    read(0, buf, 100) 系统调用
    → consoleread(user_dst, dst, 100)
         → 等 cons.r != cons.w（等 consoleintr 把数据放进 cons.buf）
         → 从 cons.buf[] 逐字节拷到用户 buf
         → 遇到 '\n' 返回一行

```
```
基本正确，但 RISC-V 写路径需要拆开看，不是一个东西。

  修正后的对比

                      RISC-V                          LoongArch
                      ────────                        ──────────

  读（输入）：
    驱动方式           UART RX 中断                     UART RX 中断 + 10ms 轮询
    存入              cons.buf[]                       cons.buf[]
    用户取             read() → consoleread()           read() → consoleread()

  写（printf 路径）：
    驱动方式           LSR 忙等                         LSR 忙等
    同步保护           无（关中断 push_off）             无（关中断 push_off）
    调用链             printf → consputc                同
                          → hal_putchar                    → hal_putchar
                          → while(LSR); THR                → while(LSR); THR

  写（write 系统调用路径）：
    驱动方式           TX 中断 + sleep                   LSR 忙等 ← 区别在这
    同步保护           tx_lock + tx_busy                 无锁
    调用链             write → consolewrite              同
                          → hal_console_write               → hal_console_write
                          → acquire + sleep                  → 循环 hal_putchar
                          → 被 TX 中断唤醒                   → while(LSR); THR(忙等)

  关键纠正

  RISC-V 的 write() 路径不用忙等。 它发一个字节，标记 tx_busy=1，然后 sleep(&tx_chan) 让出 CPU。等 UART 硬件发完这个字节 → TX 中断 →
  hal_console_intr 检测到 LSR_TX_IDLE → tx_busy=0 → wakeup(&tx_chan) → 进程被唤醒 → 发下一个字节。

  LoongArch 的 write() 路径是忙等。 hal_console_write 直接循环调用 hal_putchar，每个字节都 while(LSR); THR。写 1000 字节 = 1000
  次忙等，全程占着 CPU。

  hal_putchar 两架构相同，都是忙等。 因为 printf 可能在中断上下文中调用，不能 sleep——只能用忙等。
```

LoongArch 架构下的 UART 硬件（通常集成在 7A1000 桥片或 3A5000 芯片中）是完全支持 **TX（发送）中断**的。以下是针对你问题的详细调查与机制分析：

### 1. LoongArch UART TX 中断支持调查
虽然许多实验性内核版本未补全 TX 中断链路，但这并非硬件限制，而是软件实现的权衡。

*   **硬件能力**：LoongArch 使用的 16550A 兼容 UART 具备 16 位可编程时钟计数器，支持全双工收发。
*   **寄存器支持**：在 **IER（中断使能寄存器）** 中，**Bit 1 (ETBEI/ITxE)** 专门用于控制“发送保持寄存器空”中断。
*   **软件选择**：许多 OS 在控制台输出时选择**轮询模式**（不断检查 `LSR[THRE]`），是因为控制台打印通常字符量小且非连续高频，轮询实现更简单。若要实现高效的异步大数据块发送，则必须通过设置 IER Bit 1 来启用 TX 中断链路。

### 2. TX、RX 与 LSR 的中断协作机制
中断的触发与清除是 **LSR（现场状态）**、**IER（开关控制）** 与 **IIR（身份识别）** 三者紧密协作的过程：

#### **LSR (Line Status Register)：现场证人**
LSR 实时反映硬件状态，是所有逻辑的源头：
*   **RX 协作**：当 FIFO 接收到数据，硬件将 **`LSR[DR]` (Bit 0)** 置 1。
*   **TX 协作**：当发送 FIFO/THR 变空，硬件将 **`LSR[THRE]` (Bit 5)** 置 1。

#### **IER (Interrupt Enable Register)：门禁闸机**
IER 决定 LSR 的状态是否能转化为向 CPU 报错的 IRQ 信号：
*   **协作逻辑**：只有当 `LSR[DR] == 1` 且 `IER[ERBFI] == 1` 时，UART 才会拉高 IRQ 引脚。
*   **发送逻辑**：只有当 `LSR[THRE] == 1` 且 `IER[ETBEI] == 1` 时，才会触发发送中断。

#### **IIR (Interrupt Identification Register)：分拣员**
当 CPU 进入中断处理程序后，通过读取 IIR 来区分当前是 RX 还是 TX 触发的：
*   **RX 响应**：IIR 返回 `010`（接收到数据）。软件读取 **RHR** 寄存器取走字符，硬件随后自动清除 `LSR[DR]` 标志，IRQ 信号随之降低。
*   **TX 响应**：IIR 返回 `001`（发送寄存器空）。软件向 **THR** 写入新字符，硬件自动清除该中断标志。

**总结协作流程**：
1.  **硬件事件**发生 -> **LSR** 对应位置位。
2.  **IER** 匹配使能 -> 产生 **IRQ** 信号并经过 **EXTIOI** 路由至 CPU。
3.  **CPU/内核**介入 -> 通过 **IIR** 识别类型 -> 读/写数据 -> **LSR** 状态更新并撤销 IRQ。

这种机制确保了无论是“数据到了”还是“缓冲区空了”，CPU 都能精准响应。你想试着在你的内核中通过设置 **IER Bit 1** 来手动触发并捕捉一次 TX 中断吗？


`kernel/console.c` 才负责：

- 退格处理；
- `Ctrl-U` 删除一行；
- `Ctrl-P` 显示进程；
- 输入回显；
- 行缓冲；
- 唤醒正在执行 `read()` 的进程。

因此两者的关系是：

```text
console：理解字符的用途
UART：负责字符的物理收发
```

例如 UART 收到数值 `0x7f`，UART 只负责把它交给内核；[consoleintr()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/console.c:141) 才把它理解为删除键。

**5. 一个输入字符如何进入 xv6**

假设用户按下字母 `a`：

```text
宿主机键盘
 -> QEMU 把 'a' 放进 UART 接收 FIFO
 -> UART 设置 LSR_RX_READY
 -> UART 产生 RX 中断
 -> PLIC 或 EIOINTC 通知 CPU
 -> CPU 进入 trap 汇编入口
 -> kerneltrap()/usertrap()
 -> devintr()
 -> hal_irq_claim() 得到 UART IRQ
 -> hal_console_intr(consoleintr)
 -> uart_getc() 读取 RHR
 -> consoleintr('a')
 -> 回显字符并放入 cons.buf
```

公共中断分发位于 [trap.c](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/trap.c:199)。

`hal_console_intr(consoleintr)` 很关键：

- `hal_console_intr()` 属于架构 UART 驱动，负责从 RHR 取字节。
- `consoleintr()` 属于通用控制台，负责解释这个字节。

传入回调函数可以避免 UART 驱动反向依赖 `kernel/console.c`。

输入普通字符时，[consoleintr()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/console.c:163) 还会调用 `consputc()` 回显，所以键盘输入才能立即显示在终端。

直到收到换行，控制台才执行：

```c
cons.w = cons.e;
wakeup(&cons.r);
```

此前睡在 [consoleread()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/console.c:85) 中的 shell 被唤醒，获得完整命令行。

**6. 一个输出字符如何离开 xv6**

用户程序执行：

```c
write(1, "a", 1);
```

调用链为：

```text
用户 write()
 -> 系统调用进入内核
 -> filewrite()
 -> consolewrite()
 -> hal_console_write()
 -> UART 驱动等待 THR 可写
 -> WriteReg(THR, 'a')
 -> QEMU UART
 -> 宿主机终端显示 a
```

[consolewrite()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/console.c:60) 每次从用户空间复制最多 32 字节，然后交给 `hal_console_write()`。

**7. 轮询和中断的区别**

轮询是 CPU 不断询问设备：

```c
while((ReadReg(LSR) & LSR_TX_IDLE) == 0)
  ;
WriteReg(THR, c);
```

相当于 CPU 一直问：“现在能写了吗？”优点是简单可靠，缺点是等待期间 CPU 不能做其他工作。

中断方式则是：

```text
CPU 把字符交给 UART
 -> CPU 去运行其他任务或睡眠
 -> UART 准备好后产生中断
 -> 中断处理函数唤醒等待者
```

优点是节省 CPU，缺点是需要锁、睡眠队列和可靠的中断投递。

**8. RISC-V UART 的发送方式**

[RISC-V hal_uart.c](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/riscv/hal_uart.c:45) 同时启用 RX 和 TX 中断。

用户 `write()` 使用中断发送：

```text
hal_console_write()
 -> 获取 tx_lock
 -> 如果 tx_busy=1，则 sleep()
 -> 写 THR
 -> 设置 tx_busy=1
 -> UART 发完后产生 TX 中断
 -> hal_console_intr()
 -> tx_busy=0
 -> wakeup()
 -> 发送下一个字符
```

这部分位于 [hal_console_write()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/riscv/hal_uart.c:77) 和 [hal_console_intr()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/riscv/hal_uart.c:139)。

但 `printf()` 和输入回显使用 `hal_putchar()` 轮询发送，因为它们可能在中断处理或 panic 中执行，不能安全地 `sleep()`。

**9. LoongArch UART 的发送方式**

[LoongArch hal_uart.c](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/loongarch/hal_uart.c:42) 只启用 RX 中断，没有启用 TX 中断。

它的 `hal_console_write()` 直接逐字符调用 `hal_putchar()`：

```text
读取 LSR bit 5
 -> 未就绪：继续轮询
 -> 已就绪：写 THR
 -> 处理下一个字符
```

因此 LoongArch 不需要：

- `tx_lock`；
- `tx_busy`；
- TX 睡眠队列；
- TX 中断唤醒。

代码更少、状态更简单，但大量输出时 CPU 会忙等待。

LoongArch 接收仍然尝试使用 EIOINTC 的 RX 中断。不过当前 QEMU 环境中 UART 外部中断投递可能不稳定，所以 [clockintr()](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/kernel/trap.c:172) 每个时钟周期额外调用一次 `hal_console_intr(consoleintr)`。

这相当于双保险：

```text
正常路径：UART RX IRQ -> 立即读取
兜底路径：定时器中断 -> 检查 LSR -> 稍后读取
```

这不表示 LSR 不可靠；当前代码正是依靠 LSR 判断是否有字符。存在疑问的是 LoongArch QEMU 的 UART IRQ 投递链。

**10. HAL 与两个 UART 驱动的关系**

HAL 不是第三个 UART，也不是运行时的中间进程。它是一份统一接口约定：

[hal_console.h](/home/xiao-yang/xv6-riscv_for_OS_function/xv6-riscv-xv6-riscv-rev5/hal/hal_console.h:8)

```c
hal_console_init();
hal_console_write();
hal_putchar();
hal_console_intr();
```

编译 RISC-V 时，这些接口链接到 `hal/riscv/hal_uart.c`；编译 LoongArch 时，链接到 `hal/loongarch/hal_uart.c`。

```text
                    kernel/console.c
                           |
                    hal_console_* 接口
                      /            \
      riscv/hal_uart.c              loongarch/hal_uart.c
      UART 0x10000000               UART 0x1FE001E0
      PLIC IRQ 10                   EIOINTC IRQ 31
      TX 中断发送                   TX 轮询发送
```

HAL 统一的是“初始化、发送、接收字符”的语义；UART 地址、中断号、发送策略和中断控制器差异留在架构实现中。



## 4. UART：一个字符如何进出 xv6

### 4.1 16550 UART 的关键寄存器

两个 QEMU 机器都提供兼容 16550 的 UART。寄存器按字节偏移：

| 偏移 | 读 | 写 |
| --- | --- | --- |
| 0 | RHR：接收字符 | THR：发送字符 |
| 1 | IER | IER |
| 2 | ISR | FCR |
| 3 | LCR | LCR |
| 5 | LSR | - |

当 `LCR.DLAB=1` 时，偏移 0 和 1 暂时变为波特率除数 DLL/DLM。这就是
初始化代码必须先设置 DLAB、写除数，再清除 DLAB 的原因。

LSR 中本文最重要的两位：

- bit 0：接收缓冲区中有字符。
- bit 5：发送保持寄存器为空，可以写下一个字符。

### 4.2 通用控制台与 HAL 的分工

[`kernel/console.c`](kernel/console.c) 不直接访问 UART 地址。它负责：

- 输入行缓冲；
- `Ctrl-P`、`Ctrl-U`、退格和换行；
- 唤醒等待输入的进程；
- 为 `read`/`write` 系统调用提供控制台设备。

底层动作通过 [`hal/hal_console.h`](hal/hal_console.h)：

```c
void hal_console_init(void);
void hal_console_write(const char *s, int n);
void hal_putchar(int c);
void hal_console_intr(void (*handler)(int));
```
|函数 |                  所属层     |       主要职责|
|------|-------------|----------|
|hal_irq_claim()   |     中断控制器 HAL  |  查出“哪个设备产生了中断”|
|hal_console_intr()  |   UART HAL    |      从 UART 取出字符|
|consoleintr()     |     通用控制台    |    解释字符、回显、行缓冲|
|consolewrite()      |   通用控制台    |    接收用户 write() 的数据|
|hal_console_write()  |  UART HAL     |     把字符真正交给 UART|

输出路径分为两类：

- `printf` 和输入回显调用 `hal_putchar()`，要求立即、同步输出。
- 用户 `write()` 经 `consolewrite()` 批量调用 `hal_console_write()`。

输入最终都要调用 `consoleintr(c)`，由通用控制台处理字符含义。

### 4.3 RISC-V UART 输出

文件：[`hal/riscv/hal_uart.c`](hal/riscv/hal_uart.c)

初始化过程：

1. 关闭 UART 中断。
2. 设置 DLAB，写入波特率除数。
3. 设置 8 bit 字符格式。
4. 启用并清空 FIFO。
5. 打开接收和发送相关中断。

RISC-V 的 `hal_console_write()` 使用异步发送：

```text
consolewrite()
  -> hal_console_write()
  -> 写一个字符到 THR
  -> 若 UART 正忙则 sleep()
  -> TX 中断到来
  -> hal_console_intr()
  -> 清 tx_busy 并 wakeup()
  -> 继续发送后续字符
```

这样大量用户输出不必始终占用 CPU 忙等。`hal_putchar()` 仍使用 LSR
bit 5 轮询，因为 `printf` 可能发生在中断关闭或系统异常的环境中，
此时不能依赖睡眠和未来的 TX 中断。

### 4.4 LoongArch UART 输出

文件：[`hal/loongarch/hal_uart.c`](hal/loongarch/hal_uart.c)

当前实现已经执行完整 16550 初始化：

1. `IER=0`，先关闭中断。
2. `LCR.DLAB=1`，写 DLL/DLM 波特率除数。
3. 恢复 8 bit 字符格式。
4. 启用并复位 FIFO。
5. 只打开 RX 中断。

LoongArch 的发送路径保持精简：

```text
hal_console_write()
  -> 对每个字符调用 hal_putchar()
  -> 轮询 LSR bit 5
  -> 写 THR
```

它没有 RISC-V 的 `tx_busy`、发送睡眠队列和 TX 中断状态机，代码更少，
但大量输出时会占用 CPU 忙等。对教学系统和当前输出规模，这是简单性
与吞吐量之间的明确取舍。

`hal_putchar()` 在正常情况下用 `push_off()`/`pop_off()` 包住设备操作，
避免同一 CPU 的中断输出打断当前输出；panic 时则不能依赖普通锁和
中断嵌套状态，走更直接的轮询路径。

### 4.5 UART 输入的完整链路

理想的硬件中断链路是：

```text
键盘输入
  -> QEMU 把字符放入 UART RHR
  -> LSR bit 0 置位
  -> UART 产生 RX IRQ
  -> 外部中断控制器通知 CPU
  -> trap 入口
  -> devintr()
  -> hal_irq_claim()
  -> hal_console_intr(consoleintr)
  -> 读取 RHR
  -> consoleintr(c)
  -> 放入 cons.buf，必要时 wakeup()
  -> consoleread() 返回给用户程序
```

当前 LoongArch 环境还在定时器中断处理时调用
`hal_console_intr(consoleintr)`，作为接收路径的补充轮询。这意味着：

- LSR 读取本身可以正常用于判断 RX/TX 状态。
- QEMU LoongArch 的 UART IRQ 投递链仍可能不够稳定。
- 即使某次 RX IRQ 没有到达，下一次时钟中断也会把 RHR 中的字符取走。

不要把“UART IRQ 投递存在问题”和“LSR 寄存器不可使用”混为一谈。

### 4.6 从 shell 输入到输出的例子

用户键入 `echo hi` 并回车：

```text
UART 收到字符
  -> consoleintr() 逐字放入输入队列
  -> 回车后唤醒 shell
  -> shell 的 read() 返回命令行
  -> shell 创建 echo 进程
  -> echo 调用 write()
  -> consolewrite()
  -> hal_console_write()
  -> UART THR
  -> QEMU 终端显示 hi
```

这条链同时经过硬件、HAL、通用控制台、系统调用和用户程序，是理解
整个系统分层的一个好例子。

---
## 补充：trap 相关函数

  最重要的总调用关系是：

  用户态发生 trap：
  硬件 -> uservec -> usertrap -> prepare_return -> userret -> 用户态

  内核态发生 trap：
  硬件 -> kernelvec -> kerneltrap -> kernelvec恢复 -> 原内核代码

  ### uservec：用户态 trap 的汇编入口

  uservec 不是处理系统调用业务的地方。它负责把一个尚未准备好的 CPU 环境，转换成可以执行 C 内核的环境。

  用户态刚发生 trap 时：

  - 已进入内核特权级；
  - 仍然是用户页表；
  - sp 仍指向用户栈；
  - 通用寄存器仍保存用户程序状态；
  - CPU 只自动保存异常 PC 和原因。

  因此 uservec 必须：

  找到当前进程的 trapframe
   -> 保存全部用户寄存器
   -> 取得内核栈顶
   -> 取得内核页表根
   -> 设置当前 CPU id
   -> 切换内核页表和内核栈
   -> 调用 usertrap()

  RISC-V 使用 sscratch 临时保存用户 a0，通过固定 TRAPFRAME 虚拟地址访问 trapframe，然后写 satp 切换页表，见 xv6-riscv-xv6-riscv-rev5/hal/riscv/
  hal_tramp.S:25。

  LoongArch 使用 KSave0 暂存用户 a0，使用 KSave1 获得 p->trapframe，通过 DMW0 访问，然后写 PGDL，见 xv6-riscv-xv6-riscv-rev5/hal/loongarch/hal_tramp.S:27。

  ### usertrap()：处理用户态 trap 的 C 函数

  xv6-riscv-xv6-riscv-rev5/kernel/trap.c:37 运行时，uservec 已经完成：

  - 用户寄存器保存；
  - 内核栈切换；
  - 页表切换；
  - CPU id 恢复。

  它负责判断 trap 原因：

  系统调用 -> syscall()
  设备中断 -> devintr()
  缺页异常 -> vmfault()
  未知异常 -> 标记进程 killed
  定时器中断 -> yield()

  它首先把 trap 入口切换为 kernelvec。因为此时已经在内核中，如果处理系统调用期间又来一个中断，应当进入内核 trap 路径，不能再次进入 uservec。

  处理完成后，它调用 prepare_return()，并返回用户页表根。

  ### prepare_return()：准备返回参数

  它位于 xv6-riscv-xv6-riscv-rev5/kernel/trap.c:99，不恢复寄存器，只负责准备：

  关闭中断
   -> 把下次 trap 入口设置成 uservec
   -> 写入 kernel_sp
   -> 写入 kernel_satp/PGDL
   -> 写入 usertrap 地址
   -> 写入 hart id
   -> 设置返回用户态的特权级
   -> 设置返回用户 PC

  LoongArch 还会把 p->trapframe 写入 KSave1，让下一次 uservec 能找到 trapframe。

  ### userret：恢复用户现场并返回用户态

  userret 是 uservec 的反向过程：

  切换到用户页表
   -> 刷新 TLB
   -> 找到 trapframe
   -> 恢复全部用户寄存器
   -> 执行特权返回指令

  RISC-V：

  写 satp
   -> sfence.vma
   -> 从固定 TRAPFRAME 恢复寄存器
   -> sret

  LoongArch：

  写 PGDL
   -> invtlb
   -> 从 KSave1 指向的 trapframe 恢复寄存器
   -> ertn

  sret/ertn 根据之前准备的状态，同时恢复用户 PC、用户特权级和中断状态。

  ### kernelvec：内核态 trap 的汇编入口

  内核运行期间发生定时器或设备中断，会进入 kernelvec。

  它不需要切换页表和栈，因为此时已经：

  - 使用内核页表；
  - 位于进程内核栈或调度器栈；
  - 处于内核特权级。

  所以它只需要：

  在当前内核栈上留出空间
   -> 保存 caller-saved 寄存器
   -> 调用 kerneltrap()
   -> 恢复寄存器
   -> sret/ertn

  RISC-V 实现见 xv6-riscv-xv6-riscv-rev5/hal/riscv/hal_kvec.S:12。

  LoongArch 实现见 xv6-riscv-xv6-riscv-rev5/hal/loongarch/hal_kvec.S:16，还在修改 sp 前检查是否发生内核栈 guard 页异常；如果是，则切换到紧急栈并 panic。

  ### kerneltrap()：处理内核态 trap

  xv6-riscv-xv6-riscv-rev5/kernel/trap.c:144 主要处理设备和定时器中断：

  确认 trap 来自内核态
   -> 确认硬件已关闭中断
   -> devintr()
   -> 定时器中断时可能 yield()
   -> 恢复异常 PC 和状态寄存器

  它保存并恢复 sepc/sstatus 的原因是：yield() 可能切换进程，而新进程运行期间也可能产生 trap，覆盖当前 CPU 的 trap CSR。

  最终可以把四个核心角色记成：

  uservec    = 保存用户现场，建立内核运行环境
  usertrap   = 判断并处理用户 trap 的原因
  userret    = 恢复用户现场，返回用户态
  kernelvec  = 保存内核临时现场，调用 kerneltrap



## 5. 用户态与内核态跳板

### 5.1 为什么需要 trampoline

用户程序发生 trap 的瞬间存在一个困难：

- CPU 已进入内核特权级；
- 但通用寄存器仍是用户程序的值；
- 栈指针仍指向用户栈；
- 当前页表根可能仍是用户页表；
- 还不能直接调用普通 C 函数。

因此需要一小段位置和行为都严格受控的汇编：

1. 在不破坏用户寄存器的前提下找到 trapframe。
2. 保存全部用户寄存器。
3. 换成内核栈、内核页表和 CPU id。
4. 跳入 C 函数 `usertrap()`。

返回时再反向执行。这段汇编就是 trampoline。

### 5.2 通用的用户 trap 处理

[`kernel/trap.c`](kernel/trap.c) 中的 `usertrap()` 是两个架构共享的
主要逻辑：

```text
确认 trap 来自用户态
  -> 把内核 trap 入口切为 kernelvec
  -> 保存用户 PC
  -> 判断系统调用、设备中断或页故障
  -> 必要时 yield()
  -> prepare_return()
  -> 返回用户页表根
  -> 汇编 userret 恢复用户现场
```

HAL 将 LoongArch 的异常原因转换为通用层使用的 RISC-V 风格原因值，
使 `usertrap()` 和 `devintr()` 不需要各写一套大分支。

### 5.3 RISC-V：相同虚拟地址的 trampoline

相关文件：

- [`hal/riscv/hal_tramp.S`](hal/riscv/hal_tramp.S)
- [`hal/riscv/hal_kvec.S`](hal/riscv/hal_kvec.S)
- [`kernel/trap.c`](kernel/trap.c)
- [`kernel/proc.c`](kernel/proc.c)

#### 用户进入内核

RISC-V 硬件在 trap 时：

1. `sepc` 保存用户 PC。
2. `scause` 保存原因，`stval` 保存相关地址。
3. `sstatus` 记录先前特权级和中断状态。
4. 切到 S-mode、关闭中断。
5. 跳到 `stvec`。

硬件不会自动切换 `satp`，也不会切换栈。

每个用户页表都把同一物理 trampoline 页映射到固定高虚拟地址
`TRAMPOLINE`。内核页表也在同一虚拟地址映射它。因此切换页表前后，
CPU 都能继续执行这一页代码。

`uservec` 的主要步骤：

```text
csrrw a0, sscratch, a0
  -> a0 得到固定 TRAPFRAME 地址，用户 a0 暂存在 sscratch
  -> 保存全部用户寄存器
  -> 从 trapframe 读取 kernel_sp、kernel_hartid、kernel_trap、kernel_satp
  -> sp = 进程内核栈顶
  -> tp = 当前 hart id
  -> satp = 内核页表根，刷新 TLB
  -> 跳到 usertrap()
```

这里的 `sscratch` 解决了“连一个可自由使用的临时寄存器都没有”的问题。

#### 内核返回用户

`prepare_return()`：

1. 关闭中断，避免返回准备被打断。
2. 把 `stvec` 改为 `uservec`。
3. 把内核页表根、内核栈顶、`usertrap` 地址和 hart id 写入 trapframe。
4. 设置 `sstatus`，使 `sret` 返回 U-mode。
5. 把 `sepc` 设置为用户返回 PC。

随后 `userret`：

```text
切换到用户 satp
  -> 刷新 TLB
  -> 从 TRAPFRAME 恢复用户寄存器
  -> sret
```

#### 为什么用户页表映射 TRAPFRAME

RISC-V 的 `uservec` 在切换内核页表前，需要用用户页表访问 trapframe。
所以 [`kernel/proc.c`](kernel/proc.c) 创建用户页表时映射：

- `TRAMPOLINE`：可执行，不允许用户态访问。
- `TRAPFRAME`：可读写，不允许用户态访问。

“在用户页表中”不等于“用户有权限访问”；PTE 的用户权限位仍然禁止
U-mode 访问这两个页面。

### 5.4 LoongArch：DMW0 与 KSave 替代固定 trapframe 映射

相关文件：

- [`hal/loongarch/hal_tramp.S`](hal/loongarch/hal_tramp.S)
- [`hal/loongarch/hal_kvec.S`](hal/loongarch/hal_kvec.S)
- [`hal/loongarch/arch.h`](hal/loongarch/arch.h)
- [`kernel/trap.c`](kernel/trap.c)

#### 用户进入内核

LoongArch 硬件在 trap 时：

1. `ERA` 保存用户 PC。
2. `ESTAT` 保存异常或中断原因。
3. `BADV` 保存相关错误地址。
4. `PRMD` 保存先前 PLV 和中断状态。
5. 切到 PLV0、关闭中断。
6. 跳到 `EENTRY`。

硬件不会自动切换 `PGDL`，也不会自动保存全部通用寄存器。

LoongArch 当前设计没有依赖用户页表中的固定 trampoline/trapframe
映射。进入 PLV0 后，DMW0 生效，CPU 能以直接映射方式继续执行位于低端
地址的 `uservec`。

LoongArch 的 KSave CSR 是 trap 汇编使用的安全暂存位置：

- KSave0 暂存用户 `a0`。
- KSave1 保存当前进程 trapframe 的内核地址。

`prepare_return()` 在进入用户态前预先写好 KSave1。下次 trap 到来时，
`uservec` 不需要先访问用户页表中的固定 `TRAPFRAME`，即可获得真正的
trapframe 地址。

主要步骤：

```text
KSave0 暂存用户 a0
  -> 从 KSave1 取得 p->trapframe
  -> 保存用户通用寄存器和 ERA
  -> 读取 kernel_sp、kernel_hartid、kernel_trap、kernel_pgdl
  -> sp = 进程内核栈顶
  -> tp = 当前 CPU id
  -> PGDL = 内核低地址页表根
  -> 调用 usertrap()
```

LoongArch 和 RISC-V 的寄存器集合并非完全相同。汇编层负责把
LoongArch 原生寄存器放进通用 `trapframe` 约定的槽位，通用 C 代码
因此不用理解这些 ABI 差异。

#### 内核返回用户

返回时：

```text
prepare_return()
  -> 设置 EENTRY=uservec
  -> 写好 KSave1、PRMD 和 ERA
  -> userret 写用户 PGDL
  -> invtlb 刷新 TLB
  -> 从 trapframe 恢复寄存器
  -> ertn 返回 PLV3
```

LoongArch 的 `ertn` 对应 RISC-V 的 `sret`：根据异常返回状态恢复特权级、
中断状态和 PC。

### 5.5 kernelvec 为什么与 uservec 分开

从用户态进入内核时，CPU 还没有可信内核栈和内核页表，所以需要复杂的
`uservec`。

内核执行期间发生 trap 时：

- 已在内核页表中；
- 已在当前进程的内核栈或 CPU 调度栈中；
- 只需要在当前栈上保存会被 C 函数破坏的寄存器。

所以 `kernelvec` 更短。它在当前内核栈上建立临时保存区，调用
`kerneltrap()`，恢复现场后用 `sret` 或 `ertn` 返回。

LoongArch 的 `kernelvec` 还会在正常压栈前检查内核栈 guard 页故障。
若栈溢出导致页异常，它改用每 CPU 紧急栈输出诊断并 panic，避免继续在
已经损坏的栈上调用 C 函数。

### 5.6 一次系统调用的完整路径

以用户程序调用 `write()` 为例：

```text
用户库准备系统调用号和参数
  -> RISC-V ecall / LoongArch syscall
  -> 硬件保存异常 PC、原因和先前特权级
  -> uservec 保存完整用户寄存器
  -> 切换内核栈和内核页表
  -> usertrap()
  -> syscall()
  -> sys_write()
  -> consolewrite()
  -> hal_console_write()
  -> usertrapret/prepare_return()
  -> userret 恢复寄存器
  -> sret / ertn
  -> 用户程序从系统调用之后继续
```

---

## 6. 外部中断控制器：PLIC 与 EIOINTC

### 6.1 中断控制器解决什么问题

CPU 可能连接 UART、磁盘、网卡等多个设备。中断控制器负责：

- 记录哪些设备正在请求中断；
- 屏蔽或允许某个 IRQ；
- 设置优先级；
- 把 IRQ 路由到某个 CPU；
- 告诉软件当前应处理哪个 IRQ。

HAL 对通用内核提供：

```c
void hal_irq_init(void);
void hal_irq_hart_init(void);
int  hal_irq_claim(void);
void hal_irq_complete(int irq);
```

含义分别是：

- `init`：全局控制器初始化，只做一次。
- `hart_init`：配置当前 CPU 的接收上下文，每 CPU 做一次。
- `claim`：取得一个待处理中断号。
- `complete`：通知控制器该 IRQ 已处理完。

### 6.2 RISC-V PLIC

文件：[`hal/riscv/hal_plic.c`](hal/riscv/hal_plic.c)

PLIC 是 QEMU RISC-V `virt` 机器的外部中断控制器。当前使用的主要 IRQ：

- UART0：IRQ 10。
- virtio 磁盘：IRQ 1、2、3。

`hal_irq_init()` 为这些 IRQ 设置非零优先级。

`hal_irq_hart_init()`：

1. 在当前 hart 的 S-mode 上下文中使能所需 IRQ。
2. 把优先级阈值设为 0，使所有正优先级 IRQ 都能被接收。

外部中断到达后的路径：

```text
设备置 IRQ
  -> PLIC 记录 pending
  -> PLIC 向目标 hart 发出 SEIP
  -> CPU 根据 stvec 进入 trap
  -> devintr() 识别 supervisor external interrupt
  -> hal_irq_claim() 读取 PLIC claim 寄存器
  -> 分发到 UART 或 virtio 驱动
  -> hal_irq_complete(irq) 把 IRQ 写回 complete 寄存器
```

读取 claim 寄存器会原子地选择当前最高优先级 pending IRQ。处理完成后
必须 complete，否则 PLIC 不会把该源当成一次已经结束的处理中断。

### 6.3 LoongArch EIOINTC

文件：[`hal/loongarch/hal_intr.c`](hal/loongarch/hal_intr.c)

当前磁盘是由 QEMU loader 预装到 RAM 的同步块设备，不产生 virtio
中断，因此外部控制器目前主要处理 UART IRQ 31。

`hal_irq_init()`：

1. 使能 EIOINTC 中的 UART IRQ 位。
2. 配置 IPMAP，把 UART 中断路由到 CPU 0。

`hal_irq_hart_init()`：

1. 打开当前 CPU 对 EIOINTC 的接收。
2. 在 `ECFG` 中允许硬件中断 HWI 类别。

claim 路径与 PLIC 不同：

1. 检查 `ESTAT` 中是否存在 HWI。
2. 读取当前 CPU 的 EIOINTC ISR 位图。
3. 找出第一个置位的 IRQ。
4. 通过 W1C 写回清除该位。
5. 返回 IRQ 编号。

W1C 表示“写 1 清零”。因为 claim 时已经清除了 ISR 位，
`hal_irq_complete()` 在当前 EIOINTC 实现中无需额外动作。

完整路径：

```text
UART 置 IRQ31
  -> EIOINTC 记录 ISR 位并路由到 CPU0
  -> CPU ESTAT.HWI 置位
  -> EENTRY 对应入口
  -> devintr() 看到转换后的 external interrupt
  -> hal_irq_claim() 读取并清除 ISR 位
  -> hal_console_intr(consoleintr)
  -> hal_irq_complete() 无操作
```

### 6.4 为什么定时器不走 PLIC/EIOINTC

定时器属于 CPU 本地中断源，不是外部设备 IRQ：

- RISC-V 由 `stimecmp` 和 supervisor timer interrupt 驱动。
- LoongArch 由 `TCFG`、`TICLR` 和 `ESTAT` timer 位驱动。

所以 `devintr()` 虽然统一处理外部中断和定时器中断，但只有外部中断
分支调用 `hal_irq_claim()`。

RISC-V 每次设置下一次 `stimecmp`。LoongArch 使用周期定时器配置，
中断处理时清 `TICLR`，硬件继续下一个周期。

### 6.5 通用 devintr 如何工作

[`kernel/trap.c`](kernel/trap.c) 的 `devintr()` 面对统一后的原因编码：

```text
external interrupt
  -> irq = hal_irq_claim()
  -> UART: hal_console_intr(consoleintr)
  -> virtio: 对应磁盘中断处理
  -> hal_irq_complete(irq)
  -> 返回 1

timer interrupt
  -> clockintr()
  -> 返回 2

其他原因
  -> 返回 0
```

返回值 `2` 使上层知道这是时钟中断，并在适当时机让当前进程
`yield()`。这就是时间片调度与定时器硬件之间的连接点。

### 6.6 两种控制器的关键区别

| 项目 | RISC-V PLIC | LoongArch EIOINTC |
| --- | --- | --- |
| 当前外部设备 | UART、virtio | UART |
| 待处理中断表示 | claim 寄存器 | ISR 位图 |
| 路由上下文 | 每 hart S-mode context | IPMAP/core 配置 |
| 结束处理 | 写 complete | claim 时 W1C，complete 无操作 |
| CPU 原因寄存器 | `scause`/SEIP | `ESTAT`/HWI |
| 定时器是否经过控制器 | 否 | 否 |

---

## 7. 四条链路如何连接在一起

### 7.1 启动链

```text
QEMU
  -> 链接脚本规定的入口
  -> hal_entry.S 建立每 CPU 栈
  -> hal_start.c 建立架构运行环境
  -> kernel/main.c 初始化通用子系统
  -> scheduler()
```

### 7.2 输入链

```text
终端键盘
  -> UART
  -> PLIC/EIOINTC，或 LoongArch 定时器补充轮询
  -> trap 汇编入口
  -> kernel/trap.c
  -> HAL claim 和 UART 读取
  -> kernel/console.c
  -> 用户 read()
```

### 7.3 系统调用链

```text
用户指令
  -> CPU trap 机制
  -> uservec
  -> 用户 trapframe
  -> 内核页表与内核栈
  -> usertrap()/syscall()
  -> prepare_return()
  -> userret
  -> 用户指令
```

### 7.4 时钟调度链

```text
CPU 本地定时器
  -> kernelvec 或 uservec
  -> devintr()
  -> clockintr()
  -> ticks/wakeup
  -> 必要时 yield()
  -> scheduler()
```

这四条链并非独立模块。例如 UART 中断必须依赖 boot 已设置好页表、
trap 入口和控制器；用户进程通过 UART 输出又必须经过 trampoline 进入
系统调用。

---

## 8. 建议的源码阅读顺序

不要一开始逐行硬啃汇编。按以下顺序建立“调用方需要什么”，再看架构
如何实现：

1. 阅读 [`kernel/main.c`](kernel/main.c)，掌握初始化总顺序。
2. 阅读 [`hal/hal_console.h`](hal/hal_console.h) 和
   [`hal/hal_intr.h`](hal/hal_intr.h)，只看接口含义。
3. 阅读两个架构的 `hal_memlayout.h` 和链接脚本，画出入口、RAM、UART。
4. 阅读两个 `hal_entry.S` 和 `hal_start.c`，跟踪到 `main()`。
5. 阅读 [`kernel/console.c`](kernel/console.c)，再对比两个 `hal_uart.c`。
6. 阅读 [`kernel/proc.h`](kernel/proc.h) 中的 trapframe。
7. 阅读 [`kernel/trap.c`](kernel/trap.c) 的 `usertrap()`、`kerneltrap()`、
   `devintr()` 和 `prepare_return()`。
8. 带着 trapframe 偏移表阅读两个 `hal_tramp.S`。
9. 阅读两个 `hal_kvec.S`，理解内核 trap 不需要再次切页表和栈。
10. 最后阅读 RISC-V 的 `hal_plic.c`、LoongArch 的 `hal_intr.c` 和
    两个架构的 CSR 辅助函数。

每读一个函数，都回答四个问题：

1. 进入函数时 CPU 处于哪个特权级？
2. 当前 `sp` 指向用户栈、进程内核栈还是启动/调度栈？
3. 当前页表根是谁？
4. 中断是打开还是关闭？

只要这四个状态清楚，绝大多数底层代码就不会再显得神秘。

---

## 9. 常见故障的定位方法

### 9.1 完全没有启动输出

检查：

- QEMU 装载地址是否与链接脚本入口一致。
- `_entry` 是否建立了正确且不重叠的每 CPU 栈。
- UART 基址是否正确。
- RISC-V 的 `mepc`、`MPP` 和 `mret` 是否正确。
- LoongArch 是否完成 `.data` 复制和 `.bss` 清零。

### 9.2 开分页后立刻卡死

检查：

- 内核页表是否映射当前 PC、栈、UART 和 RAM。
- 页表根格式是否正确。
- 切换页表后是否刷新 TLB。
- LoongArch 的 `PGDL`、`PGDH`、DMW0 和页表遍历参数是否一致。

### 9.3 能输出但不能输入

按中断五层开关检查：

1. UART IER 是否打开 RX。
2. LSR bit 0 是否能看到数据。
3. PLIC/EIOINTC 是否使能并正确路由 IRQ。
4. CPU 的外部中断类别和全局中断是否打开。
5. `stvec`/`EENTRY` 是否指向有效入口。

LoongArch 还应观察定时器补充轮询是否能取出字符，以区分 UART 寄存器
问题和外部 IRQ 投递问题。

### 9.4 用户程序一执行系统调用就崩溃

检查：

- `uservec` 地址和权限。
- trapframe 偏移是否与 `struct trapframe` 完全一致。
- 内核栈顶是否写入 trapframe。
- 用户/内核页表根是否正确。
- 切页表后的 TLB 刷新。
- 返回 PC：RISC-V `sepc`，LoongArch `ERA`。
- 返回特权级：RISC-V `sstatus`，LoongArch `PRMD`。
- LoongArch KSave1 是否在返回前指向当前进程 trapframe。

### 9.5 内核不断重复进入同一中断

检查：

- 设备本身的中断条件是否清除。
- PLIC 是否执行 complete。
- EIOINTC ISR 是否按 W1C 清除。
- 定时器 pending 是否清除或下一比较值是否推进。
- claim 返回的 IRQ 是否与实际设备一致。

---

## 10. 初学者应保留的核心心智模型

1. **启动代码是在搭建 C 内核所依赖的地基。**  
   入口地址、栈、可写数据、特权级和页表有一个错误，`main()` 都无法
   稳定运行。

2. **UART 驱动只搬运字节，console 才理解字符。**  
   UART 不知道退格、行缓冲或 `Ctrl-P`；这些属于通用控制台。

3. **硬件 trap 不等于现场已经保存。**  
   trampoline 的首要任务就是在几乎没有临时资源的情况下保存用户
   寄存器，并建立可信的内核运行环境。

4. **用户 trap 和内核 trap 的初始环境不同。**  
   用户 trap 需要换栈、换页表；内核 trap 已经拥有这些条件，所以入口
   更短。

5. **PLIC/EIOINTC 只负责外部 IRQ，不负责时钟。**  
   定时器是 CPU 本地中断，但最终与外部中断一起进入通用
   `devintr()`。

6. **HAL 统一的是语义，不是硬件寄存器。**  
   PLIC 的 claim/complete 和 EIOINTC 的 ISR/W1C 实现完全不同，但都能
   向通用层表达“取一个 IRQ、处理完一个 IRQ”。

掌握上述模型后，再阅读架构手册中的 CSR 位定义和汇编指令，会变成
对已有调用链补充细节，而不是面对一组彼此无关的寄存器名称。
